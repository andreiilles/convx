import React from 'react';

function FileCompressorPage({ user, onLogout, handleLoginSuccess, handleLoginError }) {
  return (
    <div>
      {/* MainPage content */}
    </div>
  );
}

export default FileCompressorPage;
