import React from 'react';

function ImageConversionPage({ user, onLogout, handleLoginSuccess, handleLoginError }) {
  return (
    <div>
        {/* MainPage content */}
    </div>
  );
}

export default ImageConversionPage;
